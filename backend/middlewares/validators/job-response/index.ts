export { };
const { index } = require('./GetJobResponseValidator');
const { show } = require('./ShowJobResponseValidator');
const { store } = require('./StoreJobResponseValidator');

module.exports = {
  index,
  show,
  store,
};
